# Delta Audit Record

Status: Draft

Output path: `audits/<repository-slug>/delta-audit-record.md`

## Target

- repository:
- local path:
- hosted URL:
- audited at:
- executor:
- audit mode:
- prior HEAD:
- present HEAD:
- delta mode: `allowed` | `upgrade-to-full`

## Change Summary

- prior tracked file count:
- present tracked file count:
- count delta percent:
- changed tracked paths:
- new untracked paths (non-ignored):

## Delta Invalidation

| Rule | Result | Notes |
|---|---|---|
| >20% inventory change | pass | blocked |
| license/security/ci/release path changed | pass | blocked |
| prior open Blocker | pass | blocked |
| audit.manifest.yml changed | pass | blocked |

## Evidence Index

Record every delta-scoped command, hosted check, and carry-forward decision used for scoring.

| Check | Claim area | Command or script | Working directory | Env overrides | Exit code / result | Transcript section |
|---|---|---|---|---|---|---|
| | | | | | | |

## Local Command Transcripts

### Transcript 1

- check:
- command:
- working directory:
- env overrides:
- exit code / result:

```text

```

## Hosted Transcripts

### Hosted Transcript 1

- claim area:
- command:
- working directory:
- env overrides:
- exit code / result:

```text

```

## Machine Evidence Bundle

Store the raw output from `scripts/run-delta-audit.*` and any rerun `scripts/collect-audit-evidence.*` execution here.
If the raw bundle shows an execution-environment artifact, preserve it and score from the explicit successful transcript row that verified the same claim.

```text

```

## G-21 Delta Read Scope

| Path | Change | Review | Notes |
|---|---|---|---|
| | added | read | |

## Gates To Rescore

List gate IDs affected by the change set. Copy forward only rows that meet `regulation/execution/RE_AUDIT_POLICY.md` carry-forward rules.

-

## Carry-Forward Rows

| Gate ID | Carried | Reason |
|---|---|---|
| | yes | |

## Final Recommendation

```text
Continue delta: yes | no - upgrade to full re-audit
Open Blockers from prior audit:
Gates rescored:
Facts confidence: high | medium | low
```
